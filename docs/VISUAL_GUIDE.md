# 🎨 CloudGrid Visual Architecture Guide

## 🗺️ Component Hierarchy

```
<CloudGrid>
├── WasmProvider (provides wasm instance to all children)
│   └── CameraProvider (provides camera controls to all children)
│       ├── <Canvas>
│       │   ├── <Stage> (Konva root)
│       │   │   └── <Layer>
│       │   │       ├── <GridDots> (worker-rendered)
│       │   │       ├── <Rect> (selection rectangle)
│       │   │       ├── <CanvasImageNode> (for each visible image)
│       │   │       │   └── <KonvaImage> (bitmap rendering)
│       │   │       └── <Transformer> (resize handles)
│       │   ├── fileInput (hidden upload)
│       │   ├── imageWorker (Web Worker)
│       │   └── gridWorker (Web Worker)
│       │
│       ├── <CompactToolbar> (tool selection)
│       │   ├── Select button
│       │   └── Pan button
│       │
│       └── <StatsPanel> (controls + stats)
│           ├── Undo/Redo buttons
│           ├── Zoom buttons
│           ├── Add Media button
│           ├── Delete button
│           ├── Sort Color button
│           └── Item count + zoom %
```

---

## 🔄 Data Flow Diagrams

### **User Moves an Image**

```
┌──────────────┐
│ User drags   │
│ image on     │
│ canvas       │
└──────┬───────┘
       │
       v
┌─────────────────────────────────────────┐
│ Konva onDragEnd event                   │
│ Canvas.tsx: handleDragEnd()             │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Get numeric ID from string ID           │
│ getNumericId('img-123') → 123           │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Call WASM function                       │
│ wasm.moveObject(123, newX, newY)        │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ WASM updates internal state:             │
│ - Object position                        │
│ - Quadtree (spatial index)               │
│ - Command history (for undo)             │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Sync WASM → React                        │
│ syncFromWasm(wasm)                       │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Read all objects from WASM:              │
│ - for (i = 0; i < count; i++)           │
│   - id = getObjectIdAtIndex(i)          │
│   - x = getObjectX(id)                  │
│   - y = getObjectY(id)                  │
│   - ... (width, height, assetId)        │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Update React state                       │
│ setImages([ { id, x, y, ... }, ... ])   │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ React re-renders Canvas                  │
│ (only affected components update)        │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Konva updates image position on screen   │
│ (visual feedback to user)                │
└──────────────────────────────────────────┘
```

---

### **Color Sort with Height Normalization**

```
┌──────────────┐
│ User clicks  │
│ "Sort Color" │
│ button       │
└──────┬───────┘
       │
       v
┌─────────────────────────────────────────┐
│ StatsPanel dispatches custom event       │
│ window.dispatchEvent('sort-by-color')   │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Canvas.tsx receives event                │
│ sortImagesByColor()                      │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 1: Normalize Heights                │
│ normalizeItemHeights()                   │
│                                          │
│ - Calculate avg height of all items     │
│ - For each item:                         │
│   - Calculate new width (aspect ratio)   │
│   - Snap to grid                         │
│ - Batch resize (100 items at a time)    │
│   wasm.beginBatchResize()                │
│   for (batch) addToBatchResize(...)      │
│   wasm.endBatchResize()                  │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 2: Calculate Color Scores           │
│                                          │
│ For each image:                          │
│   color = imageColors.get(src)           │
│   score = (R - G) / (R + G + B + 1)     │
│                                          │
│ High score = Red (top-right)             │
│ Low score = Green (bottom-left)          │
│ Mid score = Blue (middle)                │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 3: Sort by Score                    │
│ imageScores.sort((a,b) => b.score-a.score)│
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 4: Create Diagonal Grid Positions   │
│                                          │
│ diagIdx = row + (maxCol - col)          │
│                                          │
│ Position (0,49) → diagIdx = 49 (top-right)│
│ Position (39,0) → diagIdx = 39 (bottom-left)│
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 5: Map Sorted Images → Positions    │
│                                          │
│ Reddest image → Top-right position       │
│ ... (gradient) ...                       │
│ Greenest image → Bottom-left position    │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 6: Update WASM Positions            │
│                                          │
│ For each item:                           │
│   wasm.moveObject(id, newX, newY)        │
│                                          │
│ (ALL 2000 items, not just visible)      │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 7: Animate Visible Konva Nodes      │
│                                          │
│ For each visible node:                   │
│   node.to({                              │
│     x: newX,                             │
│     y: newY,                             │
│     duration: scaledDuration,            │
│     easing: Konva.Easings.EaseInOut      │
│   })                                     │
│                                          │
│ Duration scales with item count:         │
│   500 items → 1.5s                       │
│   1000 items → 2.0s                      │
│   2000 items → 3.0s (capped)             │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Step 8: Sync State After Animation       │
│                                          │
│ setTimeout(() => {                       │
│   syncFromWasm(wasm)                     │
│ }, duration + 100ms)                     │
└──────────────────────────────────────────┘
```

---

### **Image Upload with Height Normalization**

```
┌──────────────┐
│ User clicks  │
│ "Add Media"  │
└──────┬───────┘
       │
       v
┌─────────────────────────────────────────┐
│ Trigger file input click                 │
│ fileInputRef.current?.click()            │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ User selects image files                 │
│ (PNG/JPEG, single or multiple)           │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ handleFileUpload(files)                  │
│                                          │
│ For each file:                           │
│   1. Create Image() object               │
│   2. Load to get dimensions              │
│   3. Calculate aspect ratio              │
│   4. Get existing avg height             │
│   5. snapDimensionsToGrid(               │
│        avgHeight, aspectRatio, gridSize) │
│   6. Register asset & add to WASM        │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Position new images horizontally         │
│                                          │
│ Find bottommost existing image           │
│ Place new row below with GAP             │
│ Center horizontally                      │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Normalize ONLY new items                 │
│                                          │
│ normalizeItemHeights(                    │
│   newItemIds,                            │
│   existingAvgHeight                      │
│ )                                        │
│                                          │
│ (Not all 2000 items - just new ones!)   │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Select new items                         │
│ setSelectedIds(newItemIds)               │
└──────┬──────────────────────────────────┘
       │
       v
┌─────────────────────────────────────────┐
│ Animate camera to show new items         │
│                                          │
│ Calculate bounds of new items            │
│ camera.zoomToFit(bounds, padding, 500ms)│
└──────────────────────────────────────────┘
```

---

## 🏗️ WASM Internal Structure

```
┌───────────────────────────────────────────────────────────┐
│                    WASM Module                            │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ InfiniteCanvas (Main State)                         │ │
│  │                                                      │ │
│  │ - objects: Map<u32, CanvasObject>                   │ │
│  │ - objectList: Array<u32> (ordered IDs)              │ │
│  │ - nextObjectId: u32                                 │ │
│  │ - viewport: Viewport                                │ │
│  │ - grid: GridSystem                                  │ │
│  │ - history: CommandHistory                           │ │
│  │ - quadtree: Quadtree                                │ │
│  │ - stateVersion: u32                                 │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ CanvasObject                                        │ │
│  │                                                      │ │
│  │ - id: u32                                           │ │
│  │ - x, y, width, height: f32                          │ │
│  │ - assetId: u32 (maps to React src URL)             │ │
│  │ - type: u32 (0=image, 1=video, 2=text)             │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Quadtree (Spatial Index)                            │ │
│  │                                                      │ │
│  │ - bounds: AABB                                      │ │
│  │ - objects: Array<u32>                               │ │
│  │ - children: [NW, NE, SW, SE] Quadtrees             │ │
│  │                                                      │ │
│  │ Methods:                                            │ │
│  │ - insert(id): void                                  │ │
│  │ - remove(id): void                                  │ │
│  │ - queryViewport(bounds): Array<u32>                │ │
│  │ - subdivide(): void                                 │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Viewport (Camera)                                   │ │
│  │                                                      │ │
│  │ - camera: Camera { x, y, zoom }                     │ │
│  │ - canvasWidth, canvasHeight: f32                    │ │
│  │ - visibleObjects: Array<u32>                        │ │
│  │                                                      │ │
│  │ Methods:                                            │ │
│  │ - update(quadtree): void                            │ │
│  │ - getVisible(): Array<u32>                          │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ CommandHistory (Undo/Redo)                          │ │
│  │                                                      │ │
│  │ - commands: Array<Command>                          │ │
│  │ - currentIndex: i32                                 │ │
│  │                                                      │ │
│  │ Commands:                                           │ │
│  │ - MoveCommand                                       │ │
│  │ - AddObjectCommand                                  │ │
│  │ - DeleteObjectCommand                               │ │
│  │ - ResizeCommand                                     │ │
│  │ - BatchMoveCommand                                  │ │
│  │ - BatchResizeCommand                                │ │
│  │ - BatchDeleteCommand                                │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ GridSystem                                          │ │
│  │                                                      │ │
│  │ - gridSize: f32                                     │ │
│  │ - snapEnabled: boolean                              │ │
│  │                                                      │ │
│  │ Methods:                                            │ │
│  │ - snap(x, y): { x, y }                              │ │
│  │ - snapValue(value): f32                             │ │
│  └─────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────┘
```

---

## 🖼️ Rendering Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│                    Frame Rendering                          │
└─────────────────────────────────────────────────────────────┘

1. Calculate Viewport Bounds
   ┌────────────────────────────────────┐
   │ const viewportBounds = {           │
   │   x: -stagePos.x / scale,          │
   │   y: -stagePos.y / scale,          │
   │   width: dimensions.width / scale, │
   │   height: dimensions.height / scale│
   │ };                                 │
   └────────────────────────────────────┘

2. Viewport Culling (useMemo)
   ┌────────────────────────────────────┐
   │ const visibleImages = useMemo(() => {│
   │   return images.filter(img => {    │
   │     return isInBounds(img, viewport)│
   │   });                              │
   │ }, [images, scale, stagePos]);     │
   └────────────────────────────────────┘
   
   Result: 2000 images → ~50 visible images

3. Request Grid from Worker
   ┌────────────────────────────────────┐
   │ gridWorker.postMessage({           │
   │   scale,                           │
   │   stagePos,                        │
   │   width,                           │
   │   height,                          │
   │   gridSize                         │
   │ });                                │
   └────────────────────────────────────┘

4. Request Images from Worker
   ┌────────────────────────────────────┐
   │ For each visible image:            │
   │   const level = selectLevel(zoom)  │
   │   imageWorker.postMessage({        │
   │     type: 'load',                  │
   │     src: img.src,                  │
   │     level                          │
   │   });                              │
   └────────────────────────────────────┘

5. Receive ImageBitmap from Worker
   ┌────────────────────────────────────┐
   │ imageWorker.onmessage = (e) => {   │
   │   const { src, bitmap, level } = e.data;│
   │   decodedBitmaps.set(src, {        │
   │     bitmap, level                  │
   │   });                              │
   │   forceUpdate();                   │
   │ };                                 │
   └────────────────────────────────────┘

6. Render Konva Layer
   ┌────────────────────────────────────┐
   │ <Layer>                            │
   │   <GridDots bitmap={gridBitmap} /> │
   │                                    │
   │   {visibleImages.map(img => (      │
   │     <CanvasImageNode               │
   │       image={img}                  │
   │       bitmap={getBitmap(img.src)}  │
   │       isSelected={...}             │
   │       onDragEnd={...}              │
   │     />                             │
   │   ))}                              │
   │                                    │
   │   <Transformer ref={...} />        │
   │ </Layer>                           │
   └────────────────────────────────────┘

7. Konva Renders to Canvas
   ┌────────────────────────────────────┐
   │ Browser paints canvas element      │
   │ Result: 60 FPS smooth rendering    │
   └────────────────────────────────────┘
```

---

## 📊 Memory Management

```
┌─────────────────────────────────────────────────────────────┐
│               Memory Usage Breakdown                        │
└─────────────────────────────────────────────────────────────┘

Main Thread:
┌──────────────────────────────────────────┐
│ React State (~10 MB)                     │
│ - images array (2000 × 200 bytes)       │
│ - selectedIds, dimensions, etc.          │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ WASM Memory (~50 MB)                     │
│ - Objects Map (2000 × 80 bytes)          │
│ - Quadtree structure                     │
│ - Command history                        │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ ImageBitmaps - Visible Only (~50-100 MB) │
│ - ~50 visible images                     │
│ - Medium res (400px): ~2 MB each         │
│ - Closed when leaving viewport           │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ Konva Objects (~20 MB)                   │
│ - Stage, Layer, Image nodes              │
│ - Only for visible items                 │
└──────────────────────────────────────────┘

Worker Thread:
┌──────────────────────────────────────────┐
│ Blob Cache (~200-300 MB)                 │
│ - 2000 images compressed                 │
│ - Each ~100-150 KB                       │
│ - Persistent cache                       │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ Color Data (~1 MB)                       │
│ - RGB values (3 × f32 × 2000)            │
└──────────────────────────────────────────┘

Total: ~350-400 MB
(vs 6+ GB without optimizations!)
```

---

## 🎯 Critical Paths

### **Path 1: App Startup → First Paint**
```
0ms:   User loads page
10ms:  React renders CloudGrid
15ms:  CloudGrid starts loading WASM
150ms: WASM loaded + initialized
160ms: Create 2000 objects in WASM
180ms: syncFromWasm → React state
200ms: Canvas renders (only ~50 visible)
250ms: Grid worker sends first bitmap
300ms: Image worker starts loading
500ms: First images appear
2000ms: All visible images loaded
```

### **Path 2: User Pans Canvas**
```
0ms:   User drags canvas
16ms:  onDragMove fires (60 FPS)
17ms:  Update stagePos in CameraContext
18ms:  Trigger viewport culling (useMemo)
20ms:  visibleImages recalculated
22ms:  Konva re-renders new visible items
25ms:  Grid worker triggered
30ms:  Image worker loads new images
50ms:  New images appear
```

### **Path 3: User Selects & Moves Image**
```
0ms:   User clicks image
1ms:   onClick → setSelectedIds
2ms:   Transformer attaches to node
5ms:   User starts dragging
16ms:  onDragMove (60 FPS, Konva handles)
100ms: User releases
101ms: onDragEnd fires
102ms: Call wasm.moveObject
103ms: WASM updates (quadtree, history)
104ms: syncFromWasm
105ms: React re-renders
110ms: Visual feedback complete
```

---

## 🧪 Testing Strategy

### **Unit Tests** (TODO)
```
packages/primitives/
  - Vec operations
  - Box intersections
  - Snap calculations

packages/state/
  - Store operations

packages/wasm/
  - Command execute/undo
  - Quadtree insert/remove/query
  - Viewport culling
```

### **Integration Tests** (TODO)
```
packages/cloudgrid/
  - WASM ↔ React sync
  - Worker communication
  - Camera operations
  - Tool switching
```

### **E2E Tests** (TODO)
```
apps/www/
  - Load 2000 images
  - Pan and zoom
  - Select and move items
  - Undo/redo
  - Color sort
  - File upload
```

---

## 🔍 Debugging Tips

### **Debug WASM State**
```typescript
// In browser console:
const wasm = window.__WASM__;  // Expose in CloudGrid.tsx for debugging
wasm.getObjectCount();
wasm.getObjectX(123);
wasm.canUndo();
```

### **Debug React State**
```typescript
// In Canvas.tsx
console.log('Images:', images.length);
console.log('Visible:', visibleImages.length);
console.log('Selected:', selectedIds);
```

### **Debug Workers**
```typescript
// In worker
self.postMessage({ type: 'debug', data: someValue });

// In main thread
worker.onmessage = (e) => {
  if (e.data.type === 'debug') {
    console.log('Worker debug:', e.data.data);
  }
};
```

### **Performance Profiling**
```
Chrome DevTools:
1. Performance tab → Record
2. Interact with canvas
3. Stop recording
4. Look for:
   - Long tasks (>50ms)
   - Frequent re-renders
   - Worker message delays
```

---

**Happy Coding! 🚀**
